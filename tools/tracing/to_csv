#!/usr/bin/env bpftrace

BEGIN
{
	print("time,hash,ack_seq,cwnd,rate");
}

tracepoint:powertcp:new_ack
{
	printf("%llu,%u,%u,%u,%lu\n", args->time, args->hash, args->ack_seq,
		args->cwnd, args->rate);
}
